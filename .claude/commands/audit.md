Your goal is to find and fix vulnerable dependencies in this project.

Do the following:

1. Run `npm audit` to find vulnerable installed packages in this project (if any).
2. Run `npm audit fix` apply updates.
3. Run test and verify the updates didn't break anything.